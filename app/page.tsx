import { LargeButton, MiddleButton, SmallButton } from '../components/styled/button';
import { Code, Heading, Logo, Menu, Paragraph, SubHeading, Subtitle, Title } from '../components/styled/typography';

const Page = () => {
    return <main>

    </main>;
};

export default Page;